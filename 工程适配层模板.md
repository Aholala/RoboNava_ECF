# 工程适配层模板

新机器人工程只需四个项目文件：

```text
robot_config.c    实例、ID、机械尺寸、PID、超时和初始化
robot_port.c      HAL 到 ECF BSP driver ops
robot_adapter.c   DR16/裁判数据转换与当赛季安全门控
robot_tasks.c     真实周期和 Module/App 调用顺序
```

## 函数归属速查

| 需要写的函数 | 放置位置 |
| --- | --- |
| `robot_init()`、`robot_enter_safe_state()` | `robot_config.c` |
| CAN/SPI/UART/USB/PWM/Watchdog 的 HAL 包装函数 | `robot_port.c` |
| `robot_adapter_remote()`、`robot_adapter_referee()` | `robot_adapter.c` |
| `robot_control_step()`、接收任务和 Safety 任务 | `robot_tasks.c` |

不要在适配层重写：裁判 CRC/帧解析、DR16 解码、BMI088 解算、电机协议、PID、底盘运动学和 App 状态机。这些直接调用 ECF。

完整结构体和调用示例见根目录的 [移植手册](../移植手册.md)。
