# RoboNava ECF 使用与移植

ECF 只保存通用算法、协议和控制组件。MCU 句柄、CAN ID 组合、任务、优先级和机器人参数放在你的机器人工程中，不放回 ECF。

## 1. 工程目录

```text
your_robot/
├── ECF/                 # 本仓库，尽量不修改
├── Core/ Drivers/       # CubeMX/HAL 生成内容
└── User/
    ├── port/            # HAL 到 bsp_*_driver_ops_t 的适配
    ├── robot_config.c   # CAN ID、PID、限幅和对象实例
    ├── robot_init.c     # 初始化顺序
    └── task/            # RTOS 或裸机调度
```

## 2. 最小接入步骤

1. 将需要的 ECF `.c` 文件加入工程，不必一次加入全部模块。
2. 为实际使用的外设实现对应 `bsp_*_driver_ops_t`。
3. 在机器人工程中静态创建 BSP、Module 和 App 对象。
4. 按 `BSP -> Module -> App -> Safety 解锁` 的顺序初始化。
5. 在 HAL 回调中把接收数据路由给对应 Module。
6. 在任务或裸机循环中调用各组件的 `update/process/flush`。

ECF 不申请动态内存。对象、CAN 接收缓冲和裁判系统缓冲均由机器人工程长期持有。

## 3. BSP 移植规则

BSP 对象保存 `device_handle + driver_ops`。`device_handle` 指向 HAL 句柄，操作表函数负责把通用参数翻译成 HAL 调用。

```c
static bsp_status_t port_can_transmit(void *handle,
                                      const bsp_can_frame_t *frame,
                                      uint32_t timeout_ms)
{
    CAN_HandleTypeDef *hcan = handle;
    /* 在这里转换帧头并调用 HAL_CAN_AddTxMessage。 */
    return BSP_STATUS_OK;
}

static const bsp_can_driver_ops_t can_ops = {
    /* 只填写头文件要求的操作。 */
};

static bsp_can_t can1;

void robot_bsp_init(void)
{
    const bsp_can_config_t config = {
        .device_handle = &hcan1,
        .driver_ops = &can_ops,
    };
    (void)bsp_can_init(&can1, &config);
}
```

移植层负责 HAL、DMA、Cache 和中断细节；BSP、Module、App 中不要直接包含 STM32 HAL 头文件。

## 4. 数据交换与 RTOS

`app_exchange` 不依赖 FreeRTOS。单线程或外部已经串行化时：

```c
app_exchange_init(NULL);
```

FreeRTOS 任务环境可以在机器人工程中提供临界区回调：

```c
static void exchange_enter(void *context)
{
    (void)context;
    taskENTER_CRITICAL();
}

static void exchange_exit(void *context)
{
    (void)context;
    taskEXIT_CRITICAL();
}

static const app_exchange_lock_t exchange_lock = {
    .enter = exchange_enter,
    .exit = exchange_exit,
    .context = NULL,
};

app_exchange_init(&exchange_lock);
```

上述接口面向任务上下文。如果需要从 ISR 发布数据，应在机器人工程中使用 ISR 安全队列，或提供与目标 RTOS 匹配的 ISR 同步方案。

控制 App 不强制使用 `app_exchange`。单任务工程可直接传递快照：

```c
const app_imu_snapshot_t *imu = app_imu_get_snapshot(&imu_app);
app_chassis_update(&chassis_app, &chassis_command, dt_s);
app_gimbal_update(&gimbal_app, &gimbal_command, imu, dt_s);
app_shooter_update(&shooter_app, &shooter_command,
                   app_gimbal_get_feedback(&gimbal_app), dt_s);
```

多任务工程可先从 `app_exchange` 读到任务局部快照，再把该快照显式传给上述函数。这样 exchange 只负责跨任务复制，不成为控制组件的隐式依赖。

### 遥控输入适配

App 不直接依赖 DR16 或 `module_board_comm`。项目侧把任意输入源转换成 `app_remote_input_t`。DR16 的 `channel[]` 和 `dial` 已是原始值减 1024 后的整数，可直接复制：

```c
static app_remote_input_t remote_input;

void robot_update_remote_input(void)
{
    const module_dr16_process_data_t *data = module_dr16_get_data(&dr16);
    remote_input = (app_remote_input_t){0};
    if ((data == NULL) || !data->is_online) return;

    for (size_t i = 0U; i < 4U; ++i) remote_input.channel[i] = data->channel[i];
    remote_input.left_switch = (app_switch_t)data->left_switch;
    remote_input.right_switch = (app_switch_t)data->right_switch;
    remote_input.mouse_x = data->mouse_x;
    remote_input.mouse_y = data->mouse_y;
    remote_input.mouse_z = data->mouse_z;
    remote_input.mouse_left_pressed = data->mouse_left_pressed;
    remote_input.mouse_right_pressed = data->mouse_right_pressed;
    remote_input.keyboard = data->keyboard;
    remote_input.dial = data->dial;
    remote_input.sequence = data->valid_frame_count;
    remote_input.online = true;
}
```

控制周期调用 `app_command_update(&remote_input, dt_s)`。如果数据来自另一块板，只替换上述适配函数，不修改 App。底盘和发射机反馈分别通过 `app_chassis_get_feedback()` 和 `app_shooter_get_feedback()` 读取，是否经板间通信发送由项目决定。

## 5. Safety 使用

Safety 不读取 RTOS tick，调用者统一传入毫秒时间。时间允许 `uint32_t` 自然回绕。

```c
static app_safety_monitor_t remote_monitor;

void robot_safety_init(void)
{
    const app_safety_monitor_config_t config = {
        .name = "remote",
        .timeout_ms = 100U,
        .required = true,
    };

    (void)app_safety_init(&watchdog);
    (void)app_safety_monitor_init(&remote_monitor, &config);
    (void)app_safety_register(&remote_monitor);

    /* 只表示操作员请求启动，required 监控器未在线时仍不会放行。 */
    app_safety_set_output_enabled(true);
}

void remote_frame_received(uint32_t now_ms)
{
    app_safety_notify_online(&remote_monitor, now_ms);
}

void safety_task_step(uint32_t now_ms)
{
    app_safety_process(now_ms);
}
```

最终放行条件为：人工解锁，并且至少一个 `required` 监控器存在且全部在线。任意 required 监控器离线时，Motor 门控会在离线回调之前关闭并撤销人工解锁；通信恢复后必须再次显式解锁。

## 6. Motor 使用

电机必须先初始化，再注册到对应总线。收到第一帧反馈并在线后才能使能。

```c
static module_dji_motor_bus_t dji_bus;
static module_dji_motor_t chassis_motor;

void robot_motor_init(void)
{
    const module_dji_motor_config_t config = {
        .name = "mecanum_1",
        .motor_bus = &dji_bus,
        .motor_model = MODULE_DJI_MOTOR_M3508,
        .control_mode = MODULE_DJI_CONTROL_VELOCITY,
        .motor_identifier = 1U,
        /* 继续填写方向、限温、电流比例、PID 等实机参数。 */
    };

    (void)module_dji_motor_bus_init(&dji_bus, &can1, 1U);
    (void)module_dji_motor_init(&chassis_motor, &config);
    (void)module_dji_motor_register(&chassis_motor);
}
```

安全门控关闭时：

- `module_motor_enable` 和 `module_motor_set_target` 返回 `MODULE_MOTOR_STATUS_OUTPUT_INHIBITED`。
- DJI 总线发送零命令并清除旧命令状态。
- DM 允许发送失能命令，但拒绝使能命令。
- 重新放行后仍需重新使能并设置目标。

推荐名称由具体机器人工程填写，不在 ECF 中硬编码：

```text
gimbal_yaw, gimbal_pitch
shooter_motor
mecanum_1 ... mecanum_4
omni_1 ... omni_3/omni_4
swerve_drive_1 ... swerve_drive_4
swerve_steer_1 ... swerve_steer_4
```

`last_delta_time_s` 保存最近一次成功更新周期，`enabled_runtime_us` 只累计使能且更新成功的时间。可直接在 Ozone 中观察，也可通过 `module_motor_get_*()` 读取。

## 7. 推荐周期

| 工作 | 典型周期 | 调用位置 |
|---|---:|---|
| IMU 读取与 EKF | 1 ms | IMU 任务 |
| 电机控制与 CAN flush | 1 ms | 控制任务 |
| DR16/视觉处理 | 收到数据时 | 回调或通信任务 |
| Safety process | 5 ms | 高优先级安全任务 |
| 裁判系统解析 | 5–10 ms | 通信任务 |

周期只是起点，应根据实际总线负载和控制带宽调整；`delta_time_s` 使用实测时间，不要假定任务永远准时。

## 8. 回调路由

HAL 回调只负责确认来源并转交，不在中断中执行控制算法：

```text
HAL CAN/FDCAN RX callback
        -> bsp 接收适配
        -> DJI/DM/board dispatcher
        -> module_*_handle_feedback()
        -> safety heartbeat
```

USART/USB/SPI DMA 回调采用相同原则。H7 使用 DMA 时，缓冲区位置、对齐和 D-Cache 维护由 Port 负责。

## 9. 验证

PC 上运行：

```powershell
./Tests/run_tests.ps1
```

当前检查数据交换锁、Safety 超时和门控、Motor 失能、裁判 CRC，以及 IMU 静止更新。移植到新板后还应分别验证 CAN 零帧、DM 失能帧、BMI088 坐标方向和真实任务周期。

## 10. 常见错误

- 没有调用 `app_safety_set_output_enabled(true)`：电机始终被禁止。
- 没有 required 监控器：Safety 不会放行。
- 电机尚未收到反馈就使能：返回反馈不可用。
- `app_exchange` 在多任务中传 NULL：结构体复制可能被并发打断。
- 把 HAL 句柄或机器人 CAN ID 写入 ECF：下一次移植会再次修改框架。
- 解锁后不重新设置目标：为避免恢复旧命令，安全停机后应按正常启动流程重新使能。
