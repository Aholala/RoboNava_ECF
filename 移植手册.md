# RoboNava ECF 移植手册

## 1. 项目边界

建议把 ECF 作为只读子目录放入机器人工程：

```text
your_robot/
├─ ECF/
├─ Core/                 CubeMX/HAL 文件
└─ User/
   ├─ robot_config.c     实例、参数、CAN ID、HAL 绑定
   ├─ robot_tasks.c      Task 和调度周期
   └─ robot_port.c       BSP driver ops 与回调适配
```

不要把某台机器人的 HAL 句柄、CAN ID、底盘尺寸或 FreeRTOS 任务写回 ECF。

## 2. 移植顺序

1. 实现需要的 BSP driver ops，并初始化 CAN、SPI、UART、USB、PWM、Watchdog。
2. 初始化 Module：BMI088、DR16、DJI/DM Motor Bus、电机和裁判系统。
3. 初始化 App 实例。
4. 注册 Safety 监控器，但先保持输出禁止。
5. 执行 IMU 静止标定并等待温度稳定。
6. 收到遥控器等 required 心跳后，由操作者允许输出。
7. 启动周期任务。

每一步检查返回状态；失败时不要继续使能电机。

## 3. 遥控适配

App 不直接依赖 DR16。项目把设备数据转换为 `app_remote_input_t`：

```c
const module_dr16_process_data_t *data = module_dr16_get_data(&dr16);
app_remote_input_t remote = {0};
if (data && data->is_online) {
    for (size_t i = 0; i < 4; ++i) remote.channel[i] = data->channel[i];
    remote.left_switch = (app_switch_t)data->left_switch;
    remote.right_switch = (app_switch_t)data->right_switch;
    remote.mouse_left_pressed = data->mouse_left_pressed;
    remote.mouse_right_pressed = data->mouse_right_pressed;
    remote.keyboard = data->keyboard;
    remote.dial = data->dial;
    remote.online = true;
}
```

`channel[]` 使用减去 1024 后的原始整数，DR16 典型范围 `[-660, 660]`。

## 4. Safety

```c
app_safety_t safety;
app_safety_monitor_t remote_monitor;

app_safety_init(&safety, &watchdog);
app_safety_monitor_init(&remote_monitor, &(app_safety_monitor_config_t){
    .name = "remote", .timeout_ms = 100, .required = true,
});
app_safety_register(&safety, &remote_monitor);

/* 收到有效遥控帧 */
app_safety_notify_online(&remote_monitor, now_ms);

/* 周期任务 */
app_safety_process(&safety, now_ms);
```

`app_safety_set_output_enabled(&safety, true)` 只是操作者请求；所有 required 监控器在线后才真正允许电机输出。required 对象失联会立即撤销请求，恢复后必须重新明确允许。

## 5. IMU 与加热

```c
static void imu_set_heater(void *context, float duty)
{
    project_pwm_set_duty(context, duty); /* duty: 0..1 */
}

app_imu_config_t config = {
    .sensor = &bmi088,
    .calibration_sample_count = 1000,
    .calibration_sample_interval_ms = 1,
    .calibration_max_gyro_deviation_rad_per_s = 0.02f,
    .calibration_max_acceleration_deviation_m_per_s2 = 0.15f,
    .target_temperature_c = 40.0f,
    .temperature_tolerance_c = 0.5f,
    .temperature_stable_time_s = 2.0f,
    .heater_kp = 0.15f,
    .heater_ki = 0.02f,
    .set_heater = imu_set_heater,
    .heater_context = &heater_pwm,
};
```

上电保持设备静止，然后调用 `app_imu_calibrate()`。失败会进入 `APP_IMU_STATE_FAULT` 并关闭加热输出；修正安装或运动问题后重新初始化并标定。参数需在真机上调校，示例值不是所有安装结构的最终值。

## 6. 底盘

`app_chassis` 初始化时选择：

- `APP_CHASSIS_TYPE_MECANUM`
- `APP_CHASSIS_TYPE_OMNI`
- `APP_CHASSIS_TYPE_SWERVE`

Task 始终调用 `app_chassis_update(&chassis, command, dt_s)`。三种配置示例见 `App/app_chassis/README.md`。

## 7. 电机周期

先运行全部 App，再更新每条总线一次：

```c
app_chassis_update(...);
app_gimbal_update(...);
app_shooter_update(...);
module_dji_motor_bus_update(&dji_bus, dt_s);
module_dm_motor_bus_update(&dm_bus, dt_s);
```

不要在项目中额外调用 `module_motor_update()`。

## 8. 多任务工程

ECF 不提供全局交换层。跨任务数据使用目标 RTOS 的队列、任务通知，或由项目持有带临界区保护的快照。ISR 只复制数据或发通知，不在 ISR 中运行控制算法。

## 9. 验证清单

- 断开遥控器后电机报文为零或 DM 进入失能。
- Safety 未放行前任何 App 不能产生有效输出。
- IMU 静止标定会拒绝运动样本，加热占空比在 `0..1`。
- BMI088 轴映射与实体坐标一致。
- `dt_s` 来自真实周期，不写死为 `1` 或 `0.001`。
- DJI ID、分组帧和 DM 主从 ID 无冲突。
- 麦轮/全向轮/舵轮的轮序和方向符号与安装一致。
- 裁判系统断开时功率和热量相关功能采用安全降级。

PC 回归测试：

```powershell
./Tests/run_tests.ps1
```
