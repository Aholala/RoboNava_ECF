# RoboNava ECF 移植手册

本文说明新建 RoboMaster 工程后，哪些代码直接调用 ECF，哪些代码必须留在机器人项目中。推荐把 ECF 作为只读子目录；不要把某块开发板的 HAL 句柄、CAN ID、任务优先级或整机参数写回 ECF。

## 1. 推荐目录

```text
your_robot/
├─ ECF/                       RoboNava_ECF
├─ Core/                      CubeMX、HAL 和启动文件
└─ User/
   ├─ robot_config.h/.c       所有实例、CAN ID、机械参数和控制参数
   ├─ robot_port.h/.c         HAL 到 BSP 的适配
   ├─ robot_adapter.h/.c      设备数据转成 App 输入，并执行比赛规则门控
   └─ robot_tasks.c           Task 周期和调用顺序
```

只需这四类项目文件，不要再创建一套 Device 基类、全局交换层或 App 包装类。

## 2. 各文件放什么

| 文件 | 应放内容 | 不应放内容 |
| --- | --- | --- |
| `robot_config.c` | 静态实例、缓冲区、CAN ID、电机名、底盘尺寸、PID、超时、App 配置和初始化顺序 | HAL 回调、周期控制算法 |
| `robot_port.c` | CAN/SPI/UART/USB/PWM/Watchdog 的 driver ops，HAL 状态到 BSP 状态的转换 | 机器人模式、遥控映射、功率规则 |
| `robot_adapter.c` | DR16 到 `app_remote_input_t`、裁判数据到项目许可、Safety 心跳通知 | CRC、裁判帧解析、电机 PID |
| `robot_tasks.c` | 读取真实时间、调用 Module/App、总线发送、任务间快照保护 | 重复实例和参数、协议解析 |

ECF 已经负责：DR16 解码、裁判帧同步与 CRC、BMI088 与姿态解算、电机协议、底盘运动学，以及 command/chassis/gimbal/shooter/vision/safety 控制流程。

## 3. 最小适配层模板

下面的模板可直接放进项目的 `User/robot_adapter.h` 和 `User/robot_adapter.c`。它不是 ECF 的新抽象层，只是隔离一台机器人的设备选择和当赛季规则。

### `robot_adapter.h`

```c
#ifndef ROBOT_ADAPTER_H
#define ROBOT_ADAPTER_H

#include "app_types.h"
#include "module_dr16.h"
#include "module_referee_data.h"

typedef struct
{
    bool referee_online;
    bool chassis_power_enabled;
    bool gimbal_power_enabled;
    bool shooter_power_enabled;
    bool fire_allowed;
    float chassis_power_limit_w;
    float chassis_power_w;
    float chassis_buffer_energy_j;
} robot_referee_input_t;

void robot_adapter_remote(const module_dr16_process_data_t *source,
                          app_remote_input_t *destination);
void robot_adapter_referee(const module_referee_process_data_t *source,
                           bool referee_online,
                           robot_referee_input_t *destination);

#endif
```

### `robot_adapter.c`

```c
#include "robot_adapter.h"

#include <string.h>

void robot_adapter_remote(const module_dr16_process_data_t *source,
                          app_remote_input_t *destination)
{
    memset(destination, 0, sizeof(*destination));
    if ((source == NULL) || !source->is_online)
    {
        return;
    }

    for (size_t i = 0; i < MODULE_DR16_CHANNEL_COUNT; ++i)
    {
        destination->channel[i] = source->channel[i];
    }
    destination->left_switch = (app_switch_t)source->left_switch;
    destination->right_switch = (app_switch_t)source->right_switch;
    destination->mouse_x = source->mouse_x;
    destination->mouse_y = source->mouse_y;
    destination->mouse_z = source->mouse_z;
    destination->mouse_left_pressed = source->mouse_left_pressed;
    destination->mouse_right_pressed = source->mouse_right_pressed;
    destination->keyboard = source->keyboard;
    destination->dial = source->dial;
    destination->sequence = source->valid_frame_count;
    destination->online = true;
}

void robot_adapter_referee(const module_referee_process_data_t *source,
                           bool referee_online,
                           robot_referee_input_t *destination)
{
    memset(destination, 0, sizeof(*destination));
    if ((source == NULL) || !referee_online)
    {
        return; /* 失联默认不放行，项目可为非比赛调试另设人工模式。 */
    }

    destination->referee_online = true;
    destination->chassis_power_enabled = source->robot_status.chassis_power_enabled;
    destination->gimbal_power_enabled = source->robot_status.gimbal_power_enabled;
    destination->shooter_power_enabled = source->robot_status.shooter_power_enabled;
    destination->chassis_power_limit_w = (float)source->robot_status.chassis_power_limit_w;
    destination->chassis_power_w = source->power_heat.chassis_power_w;
    destination->chassis_buffer_energy_j =
        (float)source->power_heat.chassis_buffer_energy_j;

    /* 17 mm 1 号枪口示例。每发热量和安全余量属于当赛季/本机器人配置。 */
    const uint32_t heat_after_one_shot =
        (uint32_t)source->power_heat.shooter_17_mm_1_heat +
        ROBOT_HEAT_PER_SHOT + ROBOT_HEAT_RESERVE;
    destination->fire_allowed =
        source->robot_status.shooter_power_enabled &&
        (source->projectile_allowance.projectile_17_mm_remaining > 0U) &&
        (heat_after_one_shot <= source->robot_status.shooter_barrel_heat_limit);
}
```

在 `robot_config.h` 中定义 `ROBOT_HEAT_PER_SHOT` 和 `ROBOT_HEAT_RESERVE`，并按实际枪口选择热量字段。不要把这些赛季参数硬编码进 `module_referee`。

当前 `app_shooter_update()` 尚未直接接收裁判输入，因此项目在调用前门控命令：

```c
app_shooter_command_t shooter_command = command_output->shooter;
if (!referee_input.fire_allowed)
{
    shooter_command.fire_requested = false;
    shooter_command.automatic_fire_enabled = false;
}
if (!referee_input.shooter_power_enabled)
{
    shooter_command.friction_enabled = false;
}
app_shooter_update(&robot.shooter, &shooter_command,
                   app_gimbal_get_feedback(&robot.gimbal), dt_s);
```

底盘和云台供电禁止同理：复制本周期命令并将 `enabled = false` 或模式改为 `APP_CHASSIS_MODE_NO_FORCE`。完整功率预测/超级电容控制应单独作为项目功能实现；仅靠裁判系统测得功率做简单比例缩放不够稳定。

## 4. 初始化顺序

```text
初始化 HAL 外设
    ↓
初始化 BSP driver ops
    ↓
初始化 Module（BMI088、DR16、Referee、DJI/DM Bus 和电机）
    ↓
初始化 App 实例
    ↓
注册 Safety 监控，但保持输出禁止
    ↓
IMU 静止标定并等待温度稳定
    ↓
启动 DR16、裁判系统和周期任务
    ↓
必要心跳在线后，由操作者明确允许输出
```

每一步都检查返回值。初始化失败时保持电机失能，不继续带输出运行。

## 5. 裁判系统接入

项目只负责分配对象和缓冲区，ECF 负责协议解析。裁判串口一般使用 DMA 或中断接收，具体 USART 和 HAL 回调由 `robot_port.c` 绑定。

```c
static module_referee_t referee;
static module_referee_process_data_t referee_data;
static uint8_t referee_rx[128];
static uint8_t referee_processing[128];
static uint8_t referee_stream[256];
static uint8_t referee_tx[256];

module_referee_data_reset(&referee_data);

const module_referee_config_t config = {
    .usart = &referee_usart,
    .receive_buffer = referee_rx,
    .receive_capacity = sizeof(referee_rx),
    .processing_buffer = referee_processing,
    .processing_capacity = sizeof(referee_processing),
    .stream_buffer = referee_stream,
    .stream_capacity = sizeof(referee_stream),
    .transmit_buffer = referee_tx,
    .transmit_capacity = sizeof(referee_tx),
    .default_handler = module_referee_data_route_handler,
    .default_user_context = &referee_data,
    .receive_timeout_ms = 100,
    .transmit_timeout_ms = 100,
    .offline_timeout_ms = 500,
    .receive_mode = BSP_TRANSFER_MODE_DMA,
};

if (module_referee_init(&referee, &config) != MODULE_REFEREE_STATUS_OK ||
    module_referee_start(&referee) != MODULE_REFEREE_STATUS_OK)
{
    robot_enter_safe_state();
}
```

裁判任务每次调用 `module_referee_update()`，然后把数据仓库转换为项目许可。`update_mask` 只表示“收到新值”；未更新的字段仍保存最近一次有效值。

```c
module_referee_update(&referee, elapsed_ms);
robot_adapter_referee(&referee_data, module_referee_is_online(&referee),
                      &referee_input);

if (module_referee_is_online(&referee))
{
    app_safety_notify_online(&referee_monitor, now_ms);
}
module_referee_data_clear_updates(&referee_data);
```

参考的 ACE_ECF 裁判实现验证了协议字段与 CRC 参数；本框架保留自己的流式解析和 BSP 接口，不复制其固定 H7/USART8/HAL 全局对象写法。

## 6. DR16 与命令 App

DR16 的 `channel[]` 已经减去中心值，典型范围为 `[-660, 660]`。项目不要再次减 1024，也不需要先转为 `[-1, 1]`。

```c
module_dr16_process(&robot.dr16);
module_dr16_update_time(&robot.dr16, elapsed_ms);
robot_adapter_remote(module_dr16_get_data(&robot.dr16), &remote_input);

app_command_update(&robot.command, &remote_input,
                   app_gimbal_get_feedback(&robot.gimbal),
                   app_vision_get_target(&robot.vision), dt_s);
```

摇杆与开关如何映射到底盘、云台和射击命令由 `app_command` 处理；若某台机器人按键定义不同，优先修改或配置 `app_command`，不要改 DR16 协议模块。

## 7. 控制任务调用顺序

同一控制任务中可直接传指针。多任务时使用目标 RTOS 的队列、任务通知或项目局部临界区传递快照，不在 ECF 中恢复全局交换层。

```c
void robot_control_step(uint32_t now_ms, uint32_t elapsed_ms)
{
    const float dt_s = (float)elapsed_ms * 0.001F;

    app_safety_process(&robot.safety, now_ms);

    app_imu_update(&robot.imu, dt_s);
    app_vision_update(&robot.vision, app_imu_get_snapshot(&robot.imu), elapsed_ms);

    app_command_update(&robot.command, &remote_input,
                       app_gimbal_get_feedback(&robot.gimbal),
                       app_vision_get_target(&robot.vision), dt_s);

    const app_command_output_t *output = app_command_get_output(&robot.command);
    app_chassis_command_t chassis_command = output->chassis;
    app_gimbal_command_t gimbal_command = output->gimbal;
    app_shooter_command_t shooter_command = output->shooter;

    const bool safe = app_safety_output_allowed(&robot.safety);
    if (!safe || !referee_input.chassis_power_enabled)
    {
        chassis_command.enabled = false;
        chassis_command.mode = APP_CHASSIS_MODE_NO_FORCE;
    }
    if (!safe || !referee_input.gimbal_power_enabled)
    {
        gimbal_command.enabled = false;
    }
    if (!safe || !referee_input.fire_allowed)
    {
        shooter_command.fire_requested = false;
        shooter_command.automatic_fire_enabled = false;
    }
    if (!safe || !referee_input.shooter_power_enabled)
    {
        shooter_command.friction_enabled = false;
    }

    app_chassis_update(&robot.chassis, &chassis_command, dt_s);
    app_gimbal_update(&robot.gimbal, &gimbal_command,
                      app_imu_get_snapshot(&robot.imu), dt_s);
    app_shooter_update(&robot.shooter, &shooter_command,
                       app_gimbal_get_feedback(&robot.gimbal), dt_s);

    module_dji_motor_bus_update(&robot.dji_bus, dt_s);
    module_dm_motor_bus_update(&robot.dm_bus, dt_s);
}
```

App 和组合 Module 只设置目标。每条电机总线每周期只更新一次；项目不要额外调用 `module_motor_update()`，否则 PID、`dt` 和运行时间会重复累计。

## 8. IMU 与加热

项目通过回调把 App 的 `0..1` 占空比连接到实际 PWM：

```c
static void imu_set_heater(void *context, float duty_ratio)
{
    project_pwm_set_duty(context, duty_ratio);
}
```

配置 BMI088、EKF、静止标定样本数、温度目标、稳定时间和 PI 参数后，保持设备静止并调用：

```c
app_imu_init(&robot.imu, &imu_config);
if (app_imu_calibrate(&robot.imu) != BSP_STATUS_OK)
{
    robot_enter_safe_state();
}
```

标定成功后先进入 `WARMING`，温度稳定后进入 `READY`；只有此时 `app_imu_get_snapshot()` 的 `valid` 才为真。温控参数和轴映射必须在真机上验证。

## 9. 底盘、电机和周期

`app_chassis_init()` 选择 `APP_CHASSIS_TYPE_MECANUM`、`APP_CHASSIS_TYPE_OMNI` 或 `APP_CHASSIS_TYPE_SWERVE`，任务统一调用 `app_chassis_update()`。具体轮序、方向和几何尺寸放在 `robot_config.c`。

推荐电机名：

- 云台：`gimbal_yaw`、`gimbal_pitch`
- 拨弹：`shooter_motor`
- 麦轮/全向轮：按布局使用 `chassis_front_left` 等名称
- 舵轮：`swerve_drive_1..4`、`swerve_steer_1..4`

名称字符串的生命周期必须长于电机实例。调试时可读取 `module_motor_get_name()`、`module_motor_get_last_delta_time_s()` 和 `module_motor_get_enabled_runtime_us()`。

`dt_s` 必须来自真实单调时钟，不写死为 `1` 或 `0.001`。若任务延迟过大，应进入安全状态或限制最大 `dt`，而不是让控制器吃下异常时间步长。

## 10. Safety

至少监控遥控器；比赛工程建议同时监控裁判系统、IMU 和关键总线。`app_safety_set_output_enabled()` 只是操作者请求，全部 required 心跳在线后 `app_safety_output_allowed()` 才会为真。required 对象一旦失联，请求会被撤销；恢复后必须重新明确允许。

Safety 的最终结果必须作用在命令或总线输出上，仅调用 `app_safety_process()` 而不门控电机没有保护效果。

## 11. 上板验证清单

- 遥控断开后，DJI 发送零电流，DM 进入失能或零输出。
- 裁判系统离线、供电位关闭或热量不足时禁止发射。
- Safety 未放行前，所有 App 都不能产生有效动力输出。
- BMI088 静止标定会拒绝运动样本，加热占空比保持在 `0..1`。
- BMI088 轴映射、云台正方向和底盘轮序与实物一致。
- DJI ID、分组帧以及 DM 主从 ID 无冲突。
- 真实 `dt`、电机名和运行时间可被调试工具读取。
- 裁判 CRC 错误计数、在线超时、分包和粘包行为正常。
- PC 回归测试通过：`./Tests/run_tests.ps1`。

先用悬空轮、限流电源和低目标值完成验证，再逐步开放整机输出。
